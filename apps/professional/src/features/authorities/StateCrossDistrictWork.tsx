import {useQuery} from '@tanstack/react-query';
import {api,queryKeys} from '@haven/api-client';
import {Panel,Loading,ErrorState} from '@haven/ui';
import {AuthorityPageHeader} from './AuthorityShared';
export function StateCrossDistrictWork(){const q=useQuery({queryKey:queryKeys.stateAnalytics,queryFn:api.stateAnalytics});if(q.isPending)return <Loading/>;if(q.isError)return <ErrorState message={q.error.message} onRetry={()=>void q.refetch()}/>;return <><AuthorityPageHeader eyebrow="Maharashtra � State monitoring scope" title="Cross-district work">Coordinate aggregate workstreams that span more than one district. Individual task details remain district-scoped unless separately authorized.</AuthorityPageHeader><Panel title="Open coordination work"><div className="table-wrap" tabIndex={0}><table><caption>State-level synthetic coordination signals.</caption><thead><tr><th scope="col">Workstream</th><th scope="col">Districts involved</th><th scope="col">Open items</th><th scope="col">Status</th></tr></thead><tbody>{q.data.crossDistrictWork.map(item=><tr key={item.workstream}><th scope="row">{item.workstream}</th><td>{item.districtCount}</td><td>{item.openItems}</td><td>{item.status.replaceAll('_',' ')}</td></tr>)}</tbody></table></div></Panel></>;}